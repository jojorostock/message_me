<%= form_for(:user, html: {class: "ui form", role: "form"}, url: profile_update_path) do |f| %>
    <%= render 'form', new_form: false, f: f %>
    <%= f.button "Save Changes", class: "ui orange submit button" %>
<% end %>